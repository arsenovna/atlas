import { STORE_TOKEN } from "../constants/action-types";

const initialState = {
  token: ""
};

function rootReducer(state = initialState, action: any) {
  return state;
}

export default rootReducer;
